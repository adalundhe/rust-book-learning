// Note that we do not have to create a separate
// file for the parent module in the parent directory 
// of a folder containing child modules we wish to
// import. Instead, we can creatre a `mod.rs`
// file within the child folder. This functions
// a lot like `__init__.py` in Python.

pub mod hosting;